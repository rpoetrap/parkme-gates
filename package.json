{
  "name": "poth-parking",
  "version": "1.0.0",
  "description": "Parking system integrated with vehicle plate recognition",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "develop": "nodemon src/server.ts",
    "build": "npm run build-ts && npm run lint",
    "build-ts": "tsc",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "parking",
    "vehicle",
    "plate",
    "backpropagation"
  ],
  "author": "Rizki Putra Pamungkas",
  "license": "UNLICENSED",
  "dependencies": {
    "onoff": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^13.13.5",
    "@types/onoff": "^4.1.0",
    "eslint": "^7.0.0",
    "nodemon": "^2.0.3",
    "ts-node": "^8.10.1",
    "typescript": "^3.8.3"
  }
}
